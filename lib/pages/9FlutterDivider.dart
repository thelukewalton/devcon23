import 'package:flutter/material.dart';

import '../components/divider.dart';

class Flutter101 extends StatelessWidget {
  const Flutter101({super.key});

  @override
  Widget build(BuildContext context) {
    return const DividerPage(title: 'Flutter 101');
  }
}
