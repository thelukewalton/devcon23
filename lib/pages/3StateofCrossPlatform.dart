import 'package:flutter/material.dart';

import '../components/divider.dart';

class StateOfCrossPlatform extends StatelessWidget {
  const StateOfCrossPlatform({super.key});

  @override
  Widget build(BuildContext context) {
    return const DividerPage(title: 'State of cross-platform');
  }
}
