import 'package:flutter/material.dart';

import '../components/divider.dart';

class AccessibilityDivider extends StatelessWidget {
  const AccessibilityDivider({super.key});

  @override
  Widget build(BuildContext context) {
    return const DividerPage(title: 'Accessibility');
  }
}
